services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: docprep_demo
      POSTGRES_USER: docprep
      POSTGRES_PASSWORD: docprep
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
